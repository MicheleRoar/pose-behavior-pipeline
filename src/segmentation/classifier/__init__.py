"""segmentation/classifier -- builds the overlap classifier merge_fragments
can optionally use: extract_overlap_candidates.py dumps labeled-by-hand
feature CSVs, train_overlap_classifier.py fits weights from them."""
